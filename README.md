# 1. 总体方案设计
## 1.1 概述
本项目通过超声波传感器测量水位，压力传感器检测压力，通过测得的数据来判断是否启补水和停止补水，使用 24V 直流灯泡模拟水泵运行状态。灯泡点亮表示水泵启动，灯泡熄灭表示水泵停止。水位变化暂时通过手动移动挡板模拟
## 1.2 总体思路
总体设计方案：传感器采集 + STM32 控制判断 + 接触器执行 + 4G MQTT 远程通信

执行部分采用 24V DC 线圈交流接触器控制负载。STM32 通过逻辑电平 N 沟道 MOS 管驱动接触器线圈，接触器主触点控制 24V 直流灯泡通断，从而模拟水泵启停。

通信部分采用 Air780E 4G 开发板。STM32 通过 UART 串口与 Air780E 通信，Air780E 通过 4G 网络连接 MQTT 服务器，实现设备数据上传和远程控制命令接收。网页端或手机端可以查看水位、压力、补水状态、运行模式和报警状态，也可以下发启动、停止和参数修改命令。

系统控制逻辑以安全保护为最高优先级。当出现压力过高、水位达到上限、传感器异常或系统刚上电数据未确认有效等情况时，STM32 必须立即关闭接触器，禁止继续补水。即使网页端或本地按键发送启动命令，也不能绕过安全保护。
# 2. 功能模块
## 2.1水位检测模块
- 采用器件：`A02YYUW` 防水超声波传感器
- 功能作用：检测当前水位，计算水位百分比
- 接入方式：串口
- 工作逻辑： `A02YYUW` 进行测距，串口发送距离数据，STM32进行接收数据，通过实际距离计算水位高度，换算百分比，判断是否低于补水下限或达到停止上限
- 水位计算公式
```
水位高度：传感器到罐底的距离-实际测量距离
水位百分比 = 水位高度 / 水罐有效高度 * 100%
```
## 2.2 水位自动控制模块
- 功能作用：根据水位上下限自动控制补水启停
- 阈值：低于 `30%` 启动补水，达到 `80%` 停止补水
- 工作逻辑：获取当前水位百分比，判断水位是否低于`30%`，如果水位连续三秒低于 `30%`，启动补水，如果水位达到`80%`,停止补水
- 延时确认机制：为了避免水位数据短时间波动导致误启动，只有水位连续`3秒`低于30%，才启动补水
## 2.3 压力检测模块
- 采用器件：外接模拟压力传感器
- 功能作用：检测管路压力，用于远程控制和过压保护
- 信号处理：ADC 采样后按比例换算为压力值
- 工作逻辑：电阻分压，RC滤波，ADC采样，按分压比例还原传感器输出电压，换算压力值，判断是否过压
- 压力换算方式：
```text
压力值 = (传感器输出电压 - 0.5V) / 4.0V * 1.0MPa
```
## 2.4 压力保护模块
- 功能作用：压力高的时候立即停止补水
- 阈值： 默认`0.8MPa`，支持通过网页端修改
- 恢复条件：压力下降到 0.7MPa 以下，并持续正常 3 秒（避免压力在阈值附近波动导致接触器反复吸合和释放，系统设置恢复回差）
- 单片机现象：灯泡熄灭，系统报警，重新根据当前模式，水位状态，控制命令来判断是否补水
- 工作逻辑
	- 压力 `>= 0.8MPa`，，进入过压保护状态，同时单片机中蜂鸣器报警，报警灯点亮，OLED报警显示，网页端显示压力过高,
	- 压力 `<= 0.7MPa `,且持续 3 秒,解除过压保护吗，蜂鸣器取消报警，报警灯熄灭，OLED取消报警显示，网页端恢复正常显示,
## 2.5 水泵启停控制模块（灯泡模拟水泵）

- 采用器件：
- 功能作用：执行 STM32 的启停控制信号，实现水泵启动和停止
- 接入方式：STM32 GPIO 控制 MOS 管，MOS 管驱动接触器线圈
- 控制对象：现阶段为验证可行性，用灯泡模拟水泵
- 工作逻辑：
	- 输入高电平：
	- 输出低电平：
- 保护器件：
- 控制链路：
## 2.6 远程通信模块
- 概述：远程通信模块用于实现设备和网页端之间的数据交互。STM32 通过 UART 向 Air780E 发送 AT 指令，控制 Air780E 完成 4G 入网、MQTT 服务器连接、主题订阅和数据发布。
- 采用器件：Air780E 4G 开发板
- 通信协议：MQTT
- 功能作用：实现STM32数据上传和远程命令接收
- 接入方式：STM32 通过 UART 串口与 Air780E 通信
- 上传数据：水位，压力，补水状态，运行模式，报警状态
- 下发命令：启动，停止，模式切换，参数修改
- 通信链路：STM32->UART->Air780E->4G 网络->MQTT服务器->网页端/手机端
- 工作逻辑：设备默认每 5 秒上传一次状态数据。发生压力过高、传感器异常等故障时，应立即上传一次报警信息。
## 2.7 网页端监测与控制模块

- 采用方式：网页端或手机浏览器访问网页端
- 功能作用：远程查看设备状态，下发控制命令
- 显示内容：水位百分比，压力值，补水状态，运行模式，报警状态，通信状态
- 控制内容：启动补水，停止补水，自动模式/手动模式
- 参数设置：水位下限，水位上限，压力阈值，水管有效高度
- 通过发布命令后STM32会先判断安全条件。如果存在过压、传感器异常、水位达到上限等情况，应拒绝启动命令。
## 2.8 本地按键控制模块

- 采用器件：轻触按键，oled显示屏
- 按键数量：3个
- 按键功能：`MODE`、`START`、`STOP`；
- 功能作用：实现现场模式切换、启动、停止和报警消音；
- 接入方式：按键连接 STM32 GPIO 输入引脚，程序采用上拉输入和软件消抖方式读取按键状态。

| 按键         | 主页面功能                 | 参数设置页面功能           |
| ---------- | --------------------- | ------------------ |
| `MODE/SET` | 短按切换自动/手动；长按进入参数设置    | 短按切换参数项目；长按保存参数并退出 |
| `START/+`  | 手动模式下启动补水；自动暂停后恢复自动控制 | 增加当前参数值            |
| `STOP/-`   | 短按停止补水；长按 2 秒报警消音     | 减少当前参数值；长按放弃修改并退出  |
#### 工作逻辑

1. 系统处于自动模式时，根据水位上下限自动控制补水。
2. 系统处于手动模式时，按下 `START` 可请求启动补水，按下 `STOP` 可停止补水。
3. 自动补水过程中按下 `STOP`，系统立即关闭接触器，停止补水，并进入自动暂停状态。
4. 自动暂停后，按下 `START` 可恢复自动控制；也可通过切换一次 `MODE` 按键恢复自动模式。
5. 发生压力过高、传感器异常或水位达到上限等故障时，任何按键均不能绕过安全保护启动水泵。
6. `STOP` 按键长按时，只关闭蜂鸣器提示，不清除实际故障；故障是否解除仍由系统根据恢复条件判断。
## 2.9 报警提示模块
- 采用器件：有缘蜂鸣器，报警灯
- 功能作用：提示系统异常专挑
- 报警类型：压力过高，水位传感器异常，压力传感器异常，通信异常
- 提示方式：蜂鸣器间歇报警，报警灯点亮，网页端显示故障
# 3. 硬件系统方案

# 4. 软件系统方案

### 4.1 软件总体流程

```text
系统上电初始化
-> 初始化 GPIO、UART、ADC、I2C、定时器和看门狗
-> 读取 Flash 保存的参数
-> 等待水位与压力数据有效
-> 周期采集传感器和按键数据
-> 数据滤波、计算和异常判断
-> 执行安全保护判断
-> 执行自动或手动控制逻辑
-> 更新接触器、OLED、LED 和蜂鸣器
-> 处理 Air780E、MQTT 上传和远程命令
-> 循环运行
```
## 4.3 软件功能划分

- 传感器驱动：解析 A02YYUW 串口数据，读取 ADC 压力数据；
- 数据处理：计算水位、压力，进行滤波和范围校验；
- 控制状态机：管理自动、手动、补水运行、自动暂停、过压保护和传感器故障状态；
- 执行驱动：控制 MOS 管、蜂鸣器、LED 和 OLED；
- 按键处理：消抖、短按、长按和设置菜单；
- 通信处理：Air780E 初始化、MQTT 连接、发布、订阅和重连；
- 参数管理：校验并保存本地或网页端修改的参数；
- 看门狗：主循环正常运行时定期喂狗，异常卡死时自动复位。
# 5. 控制逻辑方案
## 5.1 工作模式

- 自动模式：按照水位上下限自动补水；
- 手动模式：由本地按键或网页端请求启动、停止；
- 自动暂停：自动补水过程中收到 STOP 命令后停止，直到收到恢复自动控制命令；
- 故障锁定：发生过压或传感器异常时，强制停止并禁止启动。
### 5.2 自动补水逻辑

```text
水位连续低于启动下限 3 秒
-> 检查压力、传感器和故障状态
-> 接触器吸合
-> 灯泡点亮
-> 模拟补水运行

水位达到停止上限
-> 接触器释放
-> 灯泡熄灭
-> 停止补水
```
### 5.3 控制优先级

```text
安全保护-> 停止命令-> 自动控制逻辑-> 本地或远程启动命令
```
# 6. 通信方案
系统采用 Air780E 4G 开发板 + MQTT 协议实现通信。设备默认每 5 秒上传一次状态数据，报警时立即上传报警信息
MQTT 主题初步设计：

|主题|方向|说明|
|---|---|---|
|water_tank_001/status|设备上传|上传水位、压力、运行状态|
|water_tank_001/alarm|设备上传|上传报警信息|
|water_tank_001/cmd|网页端下发|启动、停止、模式切换|
|water_tank_001/params|网页端下发|参数修改|

上传数据采用 JSON 格式：

```json
{
  "device_id": "water_tank_001",
  "level_percent": 65,
  "pressure_mpa": 0.32,
  "mode": "auto",
  "pump_state": "off",
  "alarm": {
    "over_pressure": false,
    "level_sensor_error": false,
    "pressure_sensor_error": false,
    "mqtt_offline": false
  }
}
```

 